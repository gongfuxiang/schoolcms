$(function()
{
	/**
	 * 登录
	 */
	$('.login-sub').on('click', function()
	{
		/*var username = $('.panel-body').find('input[name="username"]').val();
		var password = $('.panel-body').find('input[name="password"]').val();
		console.log(username);
		console.log(password);
		var data = GetFormVal('');
		Prompt('密码空的哦~');*/
	});
});